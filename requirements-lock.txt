# requirements-lock.txt — pinned runtime dependency closure (Phase F.4)
#
# HOW THIS WAS GENERATED
#   `pip freeze` from the project .venv on 2026-05-25, filtered to the runtime
#   closure of the declared deps in requirements.txt (fastapi, matplotlib,
#   numpy, pandas, pillow, python-multipart, pyyaml, scikit-learn, timm, tqdm,
#   uvicorn) plus their transitive dependencies. No development-only packages
#   were present in the environment (no jupyter/ipykernel/pytest).
#
# HOW TO REGENERATE
#   .venv/Scripts/python -m pip freeze        # Windows dev box
#   then re-apply the two edits below (PyTorch handled separately; drop unused).
#
# DELIBERATE EXCLUSIONS (present in the dev .venv but NOT in the runtime closure)
#   - torch / torchvision / torchaudio : installed SEPARATELY — see the PyTorch
#     block at the bottom of this file. The dev box uses the CUDA build
#     (torch==2.11.0+cu128, RTX 5090); a CUDA local-version tag would not
#     resolve on a CPU-only Mac, so torch is pinned by version only and
#     installed from the appropriate wheel index.
#   - torchaudio : not imported anywhere in the project.
#   - opencv-python : present in the dev .venv but not imported by the project
#     (it uses Pillow, not cv2); excluded from the runtime closure.
#
# CPU-ONLY DEPLOYMENT NOTE
#   Production/demo inference runs CPU-only (SKINLESION_DEVICE=cpu). Install
#   PyTorch BEFORE installing this file, using the commands in the PyTorch block
#   at the bottom. Then: pip install -r requirements-lock.txt

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.4.22
click==8.3.3
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
fastapi==0.115.6
filelock==3.25.2
fonttools==4.62.1
fsspec==2026.2.0
h11==0.16.0
hf-xet==1.4.3
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.12.0
idna==3.13
Jinja2==3.1.6
joblib==1.5.3
kiwisolver==1.5.0
markdown-it-py==4.0.0
MarkupSafe==3.0.3
matplotlib==3.9.3
mdurl==0.1.2
mpmath==1.3.0
networkx==3.6.1
numpy==2.1.3
packaging==26.2
pandas==2.2.3
pillow==11.0.0
pydantic==2.13.3
pydantic_core==2.46.3
Pygments==2.20.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
python-multipart==0.0.20
pytz==2026.1.post1
PyYAML==6.0.2
rich==15.0.0
safetensors==0.7.0
scikit-learn==1.5.2
scipy==1.17.1
shellingham==1.5.4
six==1.17.0
starlette==0.41.3
sympy==1.14.0
threadpoolctl==3.6.0
timm==1.0.12
tqdm==4.67.1
typer==0.24.2
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2026.2
uvicorn==0.32.1

# ----------------------------------------------------------------------------
# PyTorch (install SEPARATELY, BEFORE `pip install -r requirements-lock.txt`)
#
#   macOS (demo target — PyPI wheel is already CPU/MPS, no CUDA on Mac):
#     pip install torch==2.11.0 torchvision==0.26.0
#
#   Linux / Windows, CPU-only:
#     pip install torch==2.11.0 torchvision==0.26.0 \
#       --index-url https://download.pytorch.org/whl/cpu
#
#   Dev box reference (NOT for deployment): torch==2.11.0+cu128,
#   torchvision==0.26.0+cu128 from the cu128 index.
# ----------------------------------------------------------------------------
